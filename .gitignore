__pycache__/
*.pyc
*.zip
data.zip
Imagini dovezi/
.DS_Store
scratch/

# Triage runtime state: per-session working files, not source.
data/_review/triage_journal.jsonl
data/_review/triage_resolutions.json
*.tmp

# Pre-transformation snapshots written by the ETL scripts (~40 MB). They are
# derived copies of files git already versions, so the history is the recovery
# path; tracking them would duplicate the corpus on every transformation.
data/_backups/
